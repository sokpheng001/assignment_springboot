spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rupp_student
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
server:
  port: 8080

springdoc:
  api-docs:
    enabled: true
    path: /swagger
